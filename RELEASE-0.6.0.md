# MouseTalk 0.6.0 待上线候选

状态：**尚未公开发布**。官网当前仍提供 0.5.3；本文件记录 0.6.0 候选包已经验证和仍需人工验收的部分。

## 本版变化

- 中文正式名称统一为“妙语 MouseTalk”。
- 增加鼠标按钮到复制（⌘C）和粘贴（⌘V）的映射。
- 可选双击右键复制或粘贴；不开启时不改变普通右键行为。
- 设置页增加豆包输入法官方下载入口，并继续提供 GitHub、反馈与隐私说明。
- 已绑定额外按钮只拦截按下事件，放行抬起事件，避免鼠标状态卡住后导致弹出菜单无法点击。

## 已完成的候选包检查

- Swift 自动化测试通过。
- 生成 Apple Silicon + Intel 通用二进制。
- 校验 Info.plist、代码签名完整性、ZIP 完整性、源码归档和 SHA-256。
- 只读冲突扫描可读取系统快捷键和运行中应用菜单，并明确报告无法读取的专有驱动范围。

在干净的 `main` 上运行：

```bash
./package-release.sh
./scripts/verify-release.sh 0.6.0
```

输出位于 `dist/`，该目录不提交到 Git。

## 上线前仍需完成

- 用实际 Logitech 鼠标和豆包输入法对候选构建完成开始录音、结束录音、回车发送的人工验收。
- 在干净用户环境至少完成一次安装、首次授权、退出重开和登录后常驻检查。
- 决定正式分发签名方案。当前默认候选包是 ad-hoc 签名，未公证，Gatekeeper 会拒绝直接打开。
- 在 Intel 实机或等价环境完成启动检查；目前只有双架构构建验证。
- 官网发布前把产品名与下载版本从旧的“鼠语 / 0.5.3”统一到“妙语 / 0.6.0”。

一分钟演示的准备和镜头顺序见 [`docs/DEMO.md`](docs/DEMO.md)。
